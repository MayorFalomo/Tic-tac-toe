'use client';
import React from 'react';
import Settings from '@/components/settings/Settings';

type Props = {};

const page = (props: Props) => {
  return (
    <div>
      <Settings />
    </div>
  );
};

export default page;
