'use client';
import React from 'react';
import Settings from '@/components/settings/Settings';

const page: React.FC = () => {
  return (
    <div>
      <Settings />
    </div>
  );
};

export default page;
