import InfoMenu from '@/components/info/InfoMenu';
import React from 'react';

type Props = {};

const page = (props: Props) => {
  return (
    <div>
      <InfoMenu />
    </div>
  );
};

export default page;
